server:
  port: 7012

spring:
  application:
    name: wb-feign-provider-example

  cloud:
    nacos:
      discovery:
        enabled: true
        server-addr: 127.0.0.1:8848
        port: ${server.port}

